# PHPy

Standalone NodeJS command-line utility for analyzing and formatting PHP files.

## Installation

The tool is available as a standalone [NodeJS package `phpy`](https://www.npmjs.com/package/phpy).

_Install:_

```
npm i -g phpy
```
